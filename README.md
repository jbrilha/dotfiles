# dotfiles

## Includes dotfiles for neovim, tmux and ranger

### Neovim is using [lazy.nvim](https://github.com/folke/lazy.nvim) to setup plugins, each in their own .lua file inside the plugins folder.
